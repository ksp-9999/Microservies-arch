server:
  port: 8888
spring:
  application:
    name: ApiGateway
    
  cloud:
    gateway:
      routes:
      - id: welcomeapi
        uri: lb://WELCOMEAPI
        predicates:
        - path: /welcome
        
      - id: greetapi
        uri: lb://GREET
        predicates:
        - path: /greet

          
